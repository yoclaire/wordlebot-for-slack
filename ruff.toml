# Preserve the pre-0.16 rule set used by this repository's original CI.
# Review adoption of the expanded defaults separately from documentation changes.
# https://astral.sh/blog/ruff-v0.16.0
[lint]
select = ["E4", "E7", "E9", "F"]
